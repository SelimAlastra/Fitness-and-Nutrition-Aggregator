
// How to get a specific element from the database?
// e.g.: the object for a profile's gender/height/weight

// How to edit the properties of that object?
// e.g.: for the goals table, set the goal values

export const updateClientAnswers = (quiz) => async (dispatch) => {

    // save gender, age, height and weight

    // save the goals

    // save the profile-associated tags

}

export const updateProfessionalAnswers = (quiz) => async (dispatch) => {

    // save gender, age, height and weight

    // save the profile-associated tags

}
